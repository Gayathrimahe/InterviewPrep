# function passed to another func or variable
#needs no separate function name
# must mention lambda while representing lambda function
# as well takes many arguments but can have only one expression


n = (lambda x, y: x + y)(2, 3)
print(n)